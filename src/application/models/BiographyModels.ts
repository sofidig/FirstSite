import { Address, CompanyBasic } from "./MasterModels";

export interface Biography {
  firstName?: string;
  lastName?: string;
  // company?: CompanyBasic;
  // jobRole?: string;
  address?: Address;
  // image?: string;
}
